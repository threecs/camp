function a(){
	return 5;
}

var x = a();

console.log(x);

function checkLetter(letter){
	for(var i = 0; i < word.length; i++){
		//IF letter in Word was found
		return true;
	}
	return false;

	// Shortcut for If/else statements
	// if limited outputs
	switch(letter)
	{
		case 'a':
			// Stuff for a
		break;
		case 'b':
			// Stuff for b
		break;
		case 'c':
		case 'd':
			// Stuff for c and d
		break;
	}
	// Alternative to the Switch statement above
	if(letter == 'a'){

	}
	else if(letter == 'b')
	{

	}

}